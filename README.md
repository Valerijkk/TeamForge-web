# TeamForge – чат для программистов

> **TeamForge** – это демонстрационный чат-приложение (SPA на React) с серверной частью на Flask+Socket.IO и базой данных SQLite.  
> Поддерживает личные и групповые чаты, пересылку сообщений, ответы, реакции (лайк, сердечко), а также удаление сообщений «для всех» или «только у себя».

---

## Возможности

- **Регистрация и авторизация** пользователей (хеширование паролей).
- **Создание групповых чатов** с любым количеством участников.
- **Мгновенный обмен** сообщениями и файлами (Socket.IO).
- **Поиск** по ключевым словам в чате.
- **Реакции** на сообщения (например, «👍», «❤️»), видимые всем участникам.
- **Удаление сообщений**:
   - «Удалить у всех» – сообщение пропадает у всех пользователей чата.
   - «Удалить только у себя» – сообщение пропадает лишь у текущего пользователя.
- **Ответы и пересылка** сообщений (с отметкой «переслано от ...» или «ответ на ...»).
- **Уведомления** (их можно отключать/включать в настройках чата).
- **Профиль пользователя**: статистика по чатам, сообщениям, отправленным файлам.

---

## Установка и запуск

1. **Склонируйте репозиторий** или возьмите файлы проекта любым удобным способом.
2. **Установите зависимости** для бэкенда и фронтенда:
   - Бэкенд (в папке, где лежит `app.py`):
     ```bash
     pip install -r requirements.txt
     ```
   - Фронтенд (в папке `frontend`):
     ```bash
     npm install
     ```
3. **Запустите сервер** (Flask + Socket.IO):
   ```bash
   python app.py
   ```
   Он будет доступен на `http://localhost:5000`.
4. **Запустите фронтенд** (React):
   ```bash
   cd frontend
   npm start
   ```
   Приложение откроется на `http://localhost:3000`.

---

## Основные эндпоинты (REST)

| **Method** | **Endpoint**                         | **Описание**                                                                                               |
|------------|--------------------------------------|-------------------------------------------------------------------------------------------------------------|
| **POST**   | `/register`                          | Регистрация нового пользователя. В теле: `{ "username": "...", "password": "..." }`.                        |
| **POST**   | `/login`                             | Авторизация. Если успешно – возвращает `{ "status": "success", "user_id": ... }`.                           |
| **GET**    | `/users`                             | Список всех пользователей (для выбора при создании группового чата).                                       |
| **POST**   | `/create_chat`                       | Создание нового группового чата. В теле: `{ "name": "...", "user_ids": [...], "creator_id": ... }`.         |
| **GET**    | `/user_chats/<user_id>`              | Чаты, в которых участвует пользователь `<user_id>`.                                                         |
| **GET**    | `/messages/<chat_id>?user_id=..&q=..`| Список сообщений в чате `<chat_id>`, исключая удалённые этим пользователем. Параметр `q` – поиск по содержимому. |
| **POST**   | `/upload`                            | Загрузка файла. Ответ содержит `{"filename": "..."} – имя сохранённого файла.                               |
| **GET**    | `/uploads/<filename>`                | Получение загруженного файла по имени.                                                                      |
| **DELETE** | `/messages/<message_id>?mode=...`    | Удаление сообщения (mode=everyone – для всех, mode=me – только у себя). Параметр `user_id` обязателен.      |
| **POST**   | `/forward_message`                   | Пересылка сообщения. В теле: `{ "message_id": ..., "to_chat_id": ..., "user_id": ... }`.                    |
| **GET**    | `/profile_data/<user_id>`            | Информация о пользователе: количество чатов, сообщений и отправленных документов.                           |

---

## Основные события Socket.IO

| **Событие**               | **Описание**                                                                                                    |
|---------------------------|------------------------------------------------------------------------------------------------------------------|
| `join`                    | Пользователь «входит» в комнату чата. Все участники чата получают событие `status` ("username вошёл в чат").     |
| `leave`                   | Пользователь «выходит» из комнаты чата. Остальные получают `status` ("username покинул(а) чат").                 |
| `send_message`            | Отправка нового сообщения (сервер генерирует `receive_message`, все клиенты в чате его получат).                 |
| `receive_message`         | Приход нового сообщения от сервера (все участники чата видят сообщение).                                         |
| `send_reaction`           | Поставить реакцию на сообщение (сервер в ответ шлёт `receive_reaction`, все участники обновят реакцию).         |
| `receive_reaction`        | Приход информации о реакции на сообщение.                                                                       |
| `update_notification`     | Включение/выключение уведомлений для текущего пользователя в данном чате.                                       |
| `notification_updated`    | Сервер рассылает обновлённый статус уведомлений.                                                                |
| `message_deleted_for_all` | Сервер информирует, что сообщение удалено «для всех» (все должны убрать это сообщение из списка).                |
| `message_deleted_for_user`| Сообщение удалено «только у одного пользователя» (этот пользователь убирает сообщение; для других оно остаётся). |

---

## Таблица полей модели Message

| **Поле**            | **Тип**         | **Описание**                                                                                                                                       |
|---------------------|-----------------|-----------------------------------------------------------------------------------------------------------------------------------------------------|
| `id`                | `Integer (PK)`  | Уникальный идентификатор сообщения.                                                                                                                |
| `chat_id`           | `Integer (FK)`  | Какому чату принадлежит сообщение.                                                                                                                 |
| `sender_id`         | `Integer (FK)`  | Кто отправил сообщение.                                                                                                                            |
| `content`           | `Text`          | Текст сообщения (может быть пустым, если отправляется только файл).                                                                                |
| `timestamp`         | `DateTime`      | Время создания сообщения (по умолчанию `datetime.utcnow`).                                                                                         |
| `media_filename`    | `String(120)`   | Имя файла, если прикреплён (хранится на сервере в папке `/uploads`).                                                                                |
| `deleted_for_all`   | `Boolean`       | Флаг, что сообщение «удалено для всех» (клиентам оно не отображается).                                                                              |
| `reply_to_id`       | `Integer (FK)`  | Если это «ответ» на другое сообщение, хранит `id` того сообщения. `None`, если сообщение самостоятельное.                                          |
| `forwarded_from_id` | `Integer (FK)`  | Если сообщение «переслано», хранит `id` пользователя, от кого оно изначально. `None`, если это не пересланное сообщение.                           |

---

## Лицензия

**Лицензия (Ограничительное разрешение)**

1. **Назначение и объём**  
   Данный программный код (проект "TeamForge") предоставляется исключительно в ознакомительных и демонстрационных целях. Любое иное использование, включая, но не ограничиваясь, коммерческое использование, копирование, распространение, модификацию, интеграцию в сторонние проекты, **запрещено** без явного письменного разрешения правообладателя.

2. **Правообладатель**  
   Все авторские и смежные права на исходный код, функциональность и дизайн проекта "TeamForge" принадлежат правообладателю (далее – «Автор»).

3. **Отсутствие гарантии**  
   Данный программный код предоставляется «как есть» («as is»). Автор не даёт никаких гарантий, явных или подразумеваемых, включая гарантии товарной пригодности, пригодности для конкретных целей, а также отсутствия нарушений прав третьих лиц. Автор не несёт ответственности за возможные прямые или косвенные убытки, возникшие в результате использования или невозможности использования данного программного кода.

4. **Запрос на разрешение**  
   Для получения разрешения на любое использование проекта сверх ознакомительного (например, для обучения, демонстрации кода на публичных мероприятиях и т.п.), необходимо связаться с Автором и получить письменное согласие.

5. **Изменение условий**  
   Автор оставляет за собой право в любой момент вносить изменения в настоящую лицензию или отзывать её без предварительного уведомления.

---

© Автор проекта "TeamForge". Все права защищены.
Приятного кодинга с **TeamForge**.